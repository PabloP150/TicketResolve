# terraform plan -var-file=envs/dev/dev.tfvars  (excerpt — Deliverable C)
# EventBridge Scheduler -> escalamiento Lambda, dedicated scoped role

  # module.sla_sweep_scheduler.aws_scheduler_schedule.this will be created
  + resource "aws_scheduler_schedule" "this" {
      + arn                          = (known after apply)
      + group_name                   = "default"
      + id                           = (known after apply)
      + name                         = "ticketresolve-dev-sla-sweep"
      + name_prefix                  = (known after apply)
      + schedule_expression          = "rate(1 day)"
      + schedule_expression_timezone = "America/Guatemala"
      + state                        = "ENABLED"

      + flexible_time_window {
          + mode = "OFF"
        }

      + target {
          + arn      = (known after apply)
          + role_arn = (known after apply)
        }
    }

Plan: 95 to add, 0 to change, 0 to destroy.

Changes to Outputs:
