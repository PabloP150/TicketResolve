# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Sensitive variable files — never commit. Use dev.tfvars / prod.tfvars for non-sensitive overrides.
*.secret.tfvars
*.private.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# OS
.DS_Store
Thumbs.db
